//; my $width = parameter(Name=>'Width', Val=>4);
module `mname()` (
    input logic [`$width-1`:0] a,b,c,
    output logic[`$width-1`:0]  s, co);

   assign s = a ^ b ^c;
   assign co = a&b | b&c | a&c;

endmodule
